Sample new file, text based.

Add to git:
git add .

Remove a file:
git rm file1.txt

Commit to cloud:
git commit -m "List of changes"

Push all commits to github:
git push -u origin master

Switch branches:
git checkout gh-pages